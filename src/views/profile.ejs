<%- include('partials/head', { title }) %>
<%- include('partials/nav', { active: 'profile' }) %>
<main class="container narrow">
  <h1>Your profile</h1>
  <div id="toast" class="toast" hidden></div>

  <section class="card">
    <p class="muted">Signed in as <strong><%= currentUser.username %></strong> (<%= currentUser.role %>).</p>
    <p class="muted">Queue alerts show live on the <a href="/">dashboard</a>. For desktop
      notifications that get past bot protection, use the browser extension in <code>extension/</code>.</p>
  </section>

  <section class="card">
    <h2>Change password</h2>
    <form id="password-form" class="form-grid">
      <label class="full">Current password <input name="currentPassword" type="password" required /></label>
      <label class="full">New password <input name="newPassword" type="password" minlength="8" required /></label>
      <div class="full"><button class="btn btn-primary" type="submit">Update password</button></div>
    </form>
  </section>
</main>
<script src="/static/common.js"></script>
<script src="/static/profile.js"></script>
<%- include('partials/foot') %>
